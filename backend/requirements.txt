fastapi==0.115.6
uvicorn[standard]==0.34.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
paho-mqtt==2.1.0
SQLAlchemy==2.0.36
pydantic==2.10.4
websockets==14.1
python-multipart==0.0.20
# Optional heavy dependency — only needed when YOLO_ENABLED=true.
# Uncomment to enable real YOLO11 inference (pulls in torch).
ultralytics==8.3.40
