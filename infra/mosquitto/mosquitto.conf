# Mosquitto broker config for the Quality Inspection line.
# Open listener for the lab/demo environment — add auth for production.

persistence true
persistence_location /mosquitto/data/
log_dest stdout

# Standard MQTT
listener 1883
allow_anonymous true

# WebSocket listener (useful for browser-based MQTT clients / debugging)
listener 9001
protocol websockets
